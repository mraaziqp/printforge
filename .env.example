# Optional overrides for server/bridge.py (the defaults work for a standard local ComfyUI install)
COMFYUI_URL="http://127.0.0.1:8188"
PF_BRIDGE_HOST="127.0.0.1"
PF_BRIDGE_PORT="8000"
# Extra browser origins allowed to call the bridge, comma separated
PF_EXTRA_ORIGINS=""
